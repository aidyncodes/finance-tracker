NOTE: this code will not work if downloaded. I have pushed all changes to save but I'm currently testing.

Purpose: personal practice with Spring Boot backend and interacting with 
databases using java objects (ORM) instead of directly with mySQL. 
